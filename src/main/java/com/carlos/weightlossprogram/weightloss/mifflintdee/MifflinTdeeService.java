package com.carlos.weightlossprogram.weightloss.mifflintdee;

/* Mifflin = (10m + 6.25h - 5.0a) + s
   m is mass in kg, h is height in cm, a is age in years, s is +5 for males and -151 for females */

import javax.inject.Named;

@Named
public class MifflinTdeeService{

    int getTdee(MifflinTdeePayload payload) {
        return 0;
    }
}
